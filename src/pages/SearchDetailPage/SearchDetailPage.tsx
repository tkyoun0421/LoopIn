import { JSX } from "react";

import SearchResult from "@features/search/ui/SearchResult/SearchResult";

const SearchDetailPage = (): JSX.Element => {
  return (
    <section>
      <SearchResult />
    </section>
  );
};

export default SearchDetailPage;
